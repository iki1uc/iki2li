name: iki2li-Identitaet

on:
  workflow_run:
    workflows: ["iki7uc-Richtung"]
    types: [completed]

jobs:
  identitaet:
    runs-on: ubuntu-latest
    steps:
      - name: iki2li empfängt Kern-ID
        run: echo "iki2li: Kern-ID empfangen"

      - name: iki2li OK + VERIFY
        run: echo "iki2li: OK – VERIFY – Dienst aktiv"
