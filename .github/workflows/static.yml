name: iki2li-Identitaet

on:
  workflow_run:
    workflows: ["iki7uc-Richtung"]
    types: [completed]

jobs:
  identitaet:
    runs-on: ubuntu-latest
    steps:
      - name: Identität liefert Seil an OP
        run: echo "iki2li → OP: Identitäts-Seil übertragen"
